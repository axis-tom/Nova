# ═══════════════════════════════════════════════
# Nova — AI Agent 运营系统后端依赖
# ═══════════════════════════════════════════════

# ── FastAPI 框架 ──
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# ── 数据验证与配置 ──
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.13.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
email-validator>=2.0.0

# ── LangChain / LangGraph（AI Agent 引擎） ──
langchain-core>=1.4.0
langchain-openai>=1.2.0
langgraph>=1.2.0

# ── 数据库 ──
sqlalchemy>=2.0.0,<3.0.0
asyncpg>=0.29.0                 # PostgreSQL 异步驱动
aiosqlite>=0.19.0               # SQLite 异步驱动（本地开发/测试）
alembic>=1.13.0                 # 数据库迁移

# ── 认证 ──
pyjwt>=2.8.0
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4

# ── HTTP 客户端 ──
httpx>=0.25.0
aiohttp>=3.9.0
requests>=2.31.0

# ── 爬虫 / 数据采集 ──
beautifulsoup4>=4.14.0
duckduckgo_search>=8.1.0
feedparser>=6.0.10
aioimaplib>=1.1.0

# ── 向量数据库（RAG 记忆系统） ──
chromadb>=1.5.0

# ── 消息队列 / 缓存 ──
redis>=5.0.0

# ── 加密 ──
cryptography>=41.0.0

# ── 定时任务调度 ──
APScheduler>=3.10.0

# ── 测试 ──
pytest>=9.0.0
pytest-asyncio>=0.21.0